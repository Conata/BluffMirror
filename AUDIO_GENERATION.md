# ElevenLabs オーディオ生成ガイド

このガイドでは、ElevenLabs APIを使用してゲーム用のAI音声と効果音を生成する方法を説明します。

## 📋 必要なもの

1. **ElevenLabs APIキー**
   - `.env` ファイルに `ELEVEN_API_KEY` を設定済み ✅

2. **Python 3.7以上**

3. **必要なパッケージ**

## 🚀 セットアップ

### 1. パッケージのインストール

```bash
# プロジェクトルートディレクトリに移動
cd path/to/Baba
pip install -r requirements.txt
```

### 2. ボイスIDの選択（オプション）

デフォルトでは `Rachel` という女性ボイスを使用しています。トランプ風の男性ボイスに変更したい場合：

1. ElevenLabsのウェブサイトで利用可能なボイスを確認
2. `generate_audio.py` の `DEFAULT_VOICE_ID` を変更

```python
# generate_audio.py の14行目あたり
DEFAULT_VOICE_ID = "あなたの選んだボイスID"
```

## 🎵 オーディオ生成

### すべてのオーディオを生成

```bash
python generate_audio.py
```

これにより以下が生成されます：

#### AI音声（英語 + 日本語）
- ゲーム開始セリフ × 2
- カードドローリアクション × 3
- ペアマッチセリフ × 2
- 勝利セリフ × 2
- 敗北セリフ × 2
- 心理圧セリフ × 3

**合計**: 28ファイル（英語14 + 日本語14）

#### 効果音
- カード音: hover, pick, place, flip × 3
- 環境音: room ambience, felt slide
- 心理効果音: whisper ambience

**合計**: 9ファイル

### 出力先

```
Baba/Assets/Audio/
├── Voice/           # AI音声（.mp3）
│   ├── game_start_1.mp3
│   ├── game_start_1_ja.mp3
│   └── ...
└── SFX/            # 効果音（.mp3）
    ├── card_hover.mp3
    ├── card_pick.mp3
    └── ...
```

## 🎮 Unity への統合

### 1. ファイルのインポート

生成された音声ファイルは自動的に `Assets/Audio/` 配下に保存されます。Unityが自動的にインポートします。

### 2. AudioManagerへの割り当て

#### AI音声の設定

1. Hierarchyで `AudioManager` オブジェクトを選択
2. Inspector → Voice Group で音声クリップを割り当て

#### 効果音の設定

1. Hierarchyで `AudioManager` オブジェクトを選択
2. Inspector → 各セクションで効果音を割り当て：
   - **Card Sound Effects**:
     - Card Hover Sound: `card_hover.mp3`
     - Card Pick Sound: `card_pick.mp3`
     - Card Place Sound: `card_place.mp3`
     - Card Flip Sounds: `card_flip_1.mp3`, `card_flip_2.mp3`, `card_flip_3.mp3`

   - **Environment Sounds**:
     - Room Ambience Sound: `room_ambience.mp3`
     - Felt Slide Sound: `felt_slide.mp3`

   - **Psychology Sound Effects**:
     - Whisper Ambience Sound: `whisper_ambience.mp3`

### 3. テスト

AudioManagerを右クリック → Context Menu:
- "Test Card Hover"
- "Test Card Pick"
- "Test Heartbeat - Normal"
- など

## 📝 カスタマイズ

### セリフを追加・変更する

`generate_audio.py` の以下のセクションを編集：

```python
english_lines = {
    "your_custom_line": "Your custom text here",
    # ...
}

japanese_lines = {
    "your_custom_line_ja": "あなたのカスタムテキスト",
    # ...
}
```

### 効果音を追加・変更する

```python
sound_effects = {
    "your_sfx": ("Description of the sound effect", duration_in_seconds),
    # ...
}
```

## 💰 コスト見積もり

ElevenLabsの料金体系（2024年時点）:
- **音声合成**: 約1,000文字あたり $0.30
- **効果音生成**: 1秒あたり $0.10

このスクリプトで生成されるオーディオの概算：
- AI音声: 約2,000文字 → $0.60
- 効果音: 約15秒 → $1.50

**合計**: 約 $2.10

## 🔧 トラブルシューティング

### エラー: "ELEVEN_API_KEY が設定されていません"

→ `.env` ファイルに以下を追加:
```
ELEVEN_API_KEY=your_api_key_here
```

### エラー: "401 Unauthorized"

→ APIキーが間違っているか、期限切れです。ElevenLabsのダッシュボードで確認してください。

### エラー: "429 Too Many Requests"

→ API制限に達しました。スクリプト内の `time.sleep()` の値を大きくしてください。

### 効果音の生成に失敗する

→ ElevenLabsの Sound Effects 機能は有料プランでのみ利用可能な場合があります。プランを確認してください。

## 📚 参考リンク

- [ElevenLabs API ドキュメント](https://docs.elevenlabs.io/)
- [ElevenLabs 料金プラン](https://elevenlabs.io/pricing)
- [利用可能なボイス一覧](https://api.elevenlabs.io/v1/voices)
